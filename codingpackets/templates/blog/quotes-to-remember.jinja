{% extends "_layouts/blog.jinja" %}

{% block page_content %}

<h3 id="intro">Intro</h3>
<p>
  A great quote is worth remebering. Here are some 
  that I have heard over the years that I like to 
  keep readily available.
</p>

<h3 id="compassion" class="text-center">Compassion</h3>
{{ text::quote_block(
  attribution="Unknown",
  text="Blowing out someone else's candle doesn't make yours burn any brighter."
) }}

<h3 id="planning" class="text-center">Planning</h3>
{{ text::quote_block(
  attribution="Jeff Sutherland",
  text="The map is not the terrain."
) }}

{{ text::quote_block(
  attribution="Mike Tyson",
  text="Everyone has a plan until they get punched in the face."
) }}


<h3 id="learning" class="text-center">Learning</h3>
{{ text::quote_block(
  attribution="Albert Einstein",
  text="Education is not the learning of the facts, but the training of the mind to think."
) }}


{{ text::quote_block(
  attribution="Johny Strangler",
  text="I've forgotten more than you know."
) }}


{{ text::quote_block(
  attribution="Brad Searle",
  text="I'm not smart, i'm to stupid to give up."
) }}


<h3 id="success" class="text-center">Success</h3>
{{ text::quote_block(
  attribution="Michael Jordan",
  text="I have failed over and over again in my life
and that is why I succeed."
) }}


{{ text::quote_block(
  attribution="Unknown",
  text="Failure is a precursor to success."
) }}


{{ text::quote_block(
  attribution="David Goggins",
  text="Don't stop when you're tired. Stop when you're done."
) }}


<h3 id="sports" class="text-center">Sports</h3>
{{ text::quote_block(
  attribution="Bruce Lee",
  text="There are no limits. There are plateaus, 
but you must not stay there, you must go beyond them."
) }} 

{{ text::quote_block(
  attribution="Unknown",
  text="Pain is temporary, glory is forever."
) }} 

<h3 id="software-development" class="text-center">Software Development</h3>
{{ text::quote_block(
  attribution="Dennis Ritchie",
  text="The only way to learn a new programming 
language is by writing programs in it."
) }} 

{{ text::quote_block(
  attribution="Joe Armstrong",
  text="Make it work, then make it beautiful, then if you really, really have to, make it fast."
) }} 

<h3 id="time-management" class="text-center">Time Management</h3>
{{ text::quote_block(
  attribution="Arnold Schwarzenegger",
  text="Some people say they need 8 or 9 hours sleep 
in a day. I say SLEEP FASTER!"
) }} 

{{ text::quote_block(
  attribution="Brad Searle",
  text="Sleep is for time wasters."
) }} 

{{ text::quote_block(
  attribution="Brad Searle",
  text="'Free' time doesn't exist. Time is something you earn.
Don't waste the time you have earned."
) }} 


<h3 id="outro">Outro</h3>
<p>
  Quotes can be a great inspiration. Go forth and concquer my friends.
</p>
{% endblock page_content %}